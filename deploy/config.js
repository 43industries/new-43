/* Paste your Supabase Project URL + anon public key (INFRA.md → Shop catalog).
   Leave empty until setup is done — the public shop will show a built-in demo catalog until Supabase has published products.
   Copy from config.example.js if helpful. Never put the service_role key here. */
window.SITE_CONFIG = {
  SUPABASE_URL: '',        // https://xxxx.supabase.co
  SUPABASE_ANON_KEY: '',   // Project Settings → API → anon public
};
